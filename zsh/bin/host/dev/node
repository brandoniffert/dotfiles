#!/bin/bash

set -e

if [[ $(pwd) == "/mnt/devroot"* ]]; then
  image=node:lts

  docker run -it --rm \
    -u "$(id -u):$(id -g)" \
    -v "$PWD":/app \
    -w /app \
    $image \
    "$@"
else
  $N_PREFIX/bin/node "$@"
fi
